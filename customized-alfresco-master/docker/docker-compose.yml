version: '3.4'
services:
  dropbox-integration-share:
    image: alfresco-share-dropbox-integration:development
    build:
      dockerfile: ./Dockerfile
      context: ../../../dropbox-integration-share-docker/target
    environment:
      CATALINA_OPTS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=0.0.0.0:8888"
      REPO_HOST: dropbox-integration-acs
      REPO_PORT: 8080
    ports:
      - "${share.port}:8080"
      - "${share.debug.port}:8888"
  dropbox-integration-acs:
    image: alfresco-content-services-dropbox-integration:development
    build:
      dockerfile: ./Dockerfile
      context: ../../../dropbox-integration-platform-docker/target
    environment:
      JAVA_TOOL_OPTIONS: "${keystore.settings}"
      CATALINA_OPTS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=0.0.0.0:8888"
    ports:
      - "${acs.port}:8080"
      - "${acs.debug.port}:8888"
    volumes:
      - dropbox-integration-acs-volume:/usr/local/tomcat/alf_data
    depends_on:
      - dropbox-integration-postgres
  dropbox-integration-postgres:
    image: postgres:9.6
    environment:
      POSTGRES_DB: alfresco
      POSTGRES_USER: alfresco
      POSTGRES_PASSWORD: alfresco
    command: postgres -c max_connections=300 -c log_min_messages=LOG
    ports:
      - "${postgres.port}:5432"
    volumes:
      - dropbox-integration-db-volume:/var/lib/postgresql/data
  dropbox-integration-ass:
    image: alfresco/alfresco-search-services:1.2.0
    environment:
      SOLR_ALFRESCO_HOST: dropbox-integration-acs
      SOLR_ALFRESCO_PORT: 8080
      SOLR_SOLR_HOST: dropbox-integration-ass
      SOLR_SOLR_PORT: 8983
      SOLR_CREATE_ALFRESCO_DEFAULTS: alfresco,archive
    ports:
      - "8983:8983"
    volumes:
      - dropbox-integration-ass-volume:/opt/alfresco-search-services/contentstore
      - dropbox-integration-ass-volume:/opt/alfresco-search-services/data
volumes:
  dropbox-integration-acs-volume:
    external: true
  dropbox-integration-db-volume:
    external: true
  dropbox-integration-ass-volume:
    external: true
